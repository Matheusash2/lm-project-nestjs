generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model register {
id Int @id @default(autoincrement())
user String @unique
password String

@@map("registers")
}